# Módulo de Nómina (básica) — Plan de Implementación

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans para ejecutar tarea por tarea. Los pasos usan checkbox (`- [ ]`) para tracking.

**Goal:** Agregar a Trinity un módulo de nómina semanal/quincenal que calcula sueldos indexados en USD y pagados en Bs (a tasa BCV), con horas extra y feriados, deducciones fijas de ley (IVSS/FAOV) y descuento del crédito (CxC) del empleado, generando recibo de pago y la relación por departamento.

**Architecture:** Módulo NestJS + Next.js **aislado** (no toca ventas/caja/inventario). El empleado **reusa la ficha `Customer`** (identidad + CxC) vía `Employee.customerId`; la marca `Customer.isEmployee` ya existe. El motor de cálculo es una **función pura** replicando las fórmulas del Excel de RRHH, validada contra filas reales antes de construir UI. Migraciones **aditivas/nullable** (no afectan las nubes de una sola empresa).

**Tech Stack:** Prisma (PostgreSQL), NestJS, Next.js 14 App Router, tsx para scripts de validación, PDFKit para el recibo (ya usado en el proyecto).

---

## Contexto del Excel de RRHH (fuente de las fórmulas)

Hoja `NOMINA` (semanal), sueldo base **en USD**, todo se calcula en Bs a BCV. Fórmulas verificadas:

| Concepto | Fórmula (sin redondear intermedios) |
|---|---|
| Mensual USD | `baseUsd × periodosAño/12` (semanal=52, quincenal=24) |
| Diario USD | `mensual / 30` |
| Días totales | `díasLaborados + díasDescanso` (descanso pagado) |
| Salario período USD | `díasTotales × diario` |
| Salario Bs | `salarioPeríodoUsd × tasaBCV` |
| Valor hora Bs | `salarioBs / 40` (semanal) |
| Hora extra diurna | `valorHora × 1.5` |
| Hora extra nocturna | `horaExtraDiurna × 1.3` |
| Total HE Bs | `Ø_diurnas×valorHED + Ø_nocturnas×valorHEN` |
| IVSS / FAOV | **monto fijo Bs** (0 si no trabajó) — sobre salario mínimo, no sobre el sueldo real |
| Otras deducciones | `montoUsd × tasaBCV` |

**Regla a confirmar con RRHH (marcada en el plan):** en el Excel, "A cobrar" (col V) = `salarioBs − IVSS − FAOV − otras` y **NO suma las horas extra** (aparecen en "Total Devengado"/"Total $", cols AE/AF), con filas inconsistentes hechas a mano. El motor propuesto calcula el **neto correcto = (salario + horas extra) − deducciones**; hay que confirmar si el pago neto real incluye o no las HE, o si se pagan por separado.

**Decisiones de alcance cerradas:** sueldo indexado-USD pagado en Bs · cestaticket fuera · IVSS/FAOV/INCES como montos fijos parametrizables · frecuencias semanal + quincenal · el pago solo genera **recibo + relación** (sin tocar caja) · empleado reusa ficha `Customer`, deducción de su CxC.

---

## Plan general (fases)

| Fase | Alcance | Entregable testeable | Est. |
|---|---|---|---|
| **0 — Fundación + motor** | Schema (`Employee`, `PayrollParam`, `PayrollRun`, `PayrollRunLine`) + migración aditiva + función pura de cálculo validada contra el Excel | Script que reproduce filas reales del Excel al centavo | 1-2 ses. |
| **1 — Empleados** | Backend CRUD + frontend, reusando ficha `Customer` (`Employee.customerId`, marca `isEmployee`) | Alta/edición de empleado ligado a un cliente | 2 ses. |
| **2 — Parámetros** | UI admin de `PayrollParam` (IVSS/FAOV fijos, recargos, días base) | Editar parámetros vigentes | 1 ses. |
| **3 — Corrida** | Crear período, cargar empleados por frecuencia, capturar días/HE/deducciones, calcular con el motor, revisar, cerrar | Corrida en borrador → cerrada con totales | 2-3 ses. |
| **4 — Recibo + relación** | Recibo de pago PDF por empleado + relación agrupada por departamento con subtotales (espejo del Excel) | PDF y relación descargables | 2 ses. |
| **5 — Deducción de crédito CxC** | Jalar saldo CxC del empleado como deducción; al cerrar, aplicar contra la CxC (reusa recibos de cobro) | La deuda del empleado baja al cerrar la nómina | 1-2 ses. |

**Total ~8-10 sesiones.** Riesgo a lo existente: bajo (Fases 0-4 aisladas; solo la Fase 5 toca CxC/recibos).

---

## FASE 0 — Fundación de datos + motor de cálculo validado

Objetivo: dejar el modelo de datos y la lógica de cálculo (lo más riesgoso) probados contra números reales **antes** de cualquier UI.

### Task 1: Schema Prisma de nómina + migración

**Files:**
- Modify: `packages/database/prisma/schema.prisma` (agregar 4 modelos + relación inversa en `Customer`)
- Create: `packages/database/prisma/migrations/20260718100000_payroll_module/migration.sql`
- Modify: `deploy/fix-schema.sql` (espejo idempotente)

- [ ] **Step 1: Agregar los modelos al schema**

En `packages/database/prisma/schema.prisma`, agregar la relación inversa en `Customer` (dentro del modelo, junto a las demás relaciones):

```prisma
  employee     Employee?
```

Y al final de la sección de modelos, agregar:

```prisma
// ============================================
// NÓMINA (básica) — ver docs/superpowers/plans/2026-07-17-nomina.md
// ============================================

model Employee {
  id            String   @id @default(cuid())
  code          String?  @unique          // EMP-0001 (opcional)
  customerId    String   @unique          // ficha compartida: identidad + CxC
  customer      Customer @relation(fields: [customerId], references: [id])
  department    String                    // "ADMINISTRACION", "CAJA", "VENTAS", ...
  cargo         String?
  bank          String?
  salaryBaseUsd Float    @default(0)       // sueldo base del período, en USD
  frequency     String   @default("WEEKLY") // WEEKLY | BIWEEKLY
  isActive      Boolean  @default(true)
  lines         PayrollRunLine[]
  createdAt     DateTime @default(now())
  updatedAt     DateTime @updatedAt
}

model PayrollParam {
  id            String   @id @default("singleton")
  ivssBs        Float    @default(0)       // monto fijo IVSS por período
  faovBs        Float    @default(0)       // monto fijo FAOV por período
  incesBs       Float    @default(0)       // monto fijo INCES (0 por ahora)
  otDayFactor   Float    @default(1.5)     // recargo hora extra diurna
  otNightFactor Float    @default(1.3)     // recargo adicional nocturna sobre la diurna
  monthDays     Int      @default(30)      // base días/mes
  weeklyHours   Int      @default(40)      // horas por semana
  updatedAt     DateTime @updatedAt
}

model PayrollRun {
  id                String   @id @default(cuid())
  number            String?  @unique       // NOM-0001
  type              String                 // WEEKLY | BIWEEKLY
  periodFrom        DateTime
  periodTo          DateTime
  exchangeRate      Float                  // tasa BCV usada (snapshot)
  status            String   @default("DRAFT") // DRAFT | CLOSED
  totalGrossBs      Float    @default(0)
  totalDeductionsBs Float    @default(0)
  totalNetBs        Float    @default(0)
  lines             PayrollRunLine[]
  createdById       String
  closedAt          DateTime?
  createdAt         DateTime @default(now())
  updatedAt         DateTime @updatedAt
}

model PayrollRunLine {
  id                 String     @id @default(cuid())
  payrollRunId       String
  payrollRun         PayrollRun @relation(fields: [payrollRunId], references: [id], onDelete: Cascade)
  employeeId         String
  employee           Employee   @relation(fields: [employeeId], references: [id])
  // Entradas (snapshot)
  salaryBaseUsd      Float
  daysWorked         Float      @default(0)
  daysRest           Float      @default(0)
  overtimeDayHours   Float      @default(0)
  overtimeNightHours Float      @default(0)
  manualDeductionUsd Float      @default(0)
  creditDeductionBs  Float      @default(0)
  // Calculados
  salaryBs           Float      @default(0)
  overtimeBs         Float      @default(0)
  grossBs            Float      @default(0)
  ivssBs             Float      @default(0)
  faovBs             Float      @default(0)
  totalDeductionsBs  Float      @default(0)
  netBs              Float      @default(0)
  netUsd             Float      @default(0)

  @@unique([payrollRunId, employeeId])
}
```

- [ ] **Step 2: Crear la migración**

Crear `packages/database/prisma/migrations/20260718100000_payroll_module/migration.sql`:

```sql
-- Módulo de nómina básica. Tablas nuevas, aditivo — no afecta instalaciones existentes.
CREATE TABLE IF NOT EXISTS "Employee" (
  "id" TEXT NOT NULL, "code" TEXT, "customerId" TEXT NOT NULL, "department" TEXT NOT NULL,
  "cargo" TEXT, "bank" TEXT, "salaryBaseUsd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "frequency" TEXT NOT NULL DEFAULT 'WEEKLY', "isActive" BOOLEAN NOT NULL DEFAULT true,
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL,
  CONSTRAINT "Employee_pkey" PRIMARY KEY ("id"));
CREATE UNIQUE INDEX IF NOT EXISTS "Employee_customerId_key" ON "Employee"("customerId");
CREATE UNIQUE INDEX IF NOT EXISTS "Employee_code_key" ON "Employee"("code");
DO $$ BEGIN ALTER TABLE "Employee" ADD CONSTRAINT "Employee_customerId_fkey" FOREIGN KEY ("customerId") REFERENCES "Customer"("id") ON DELETE RESTRICT ON UPDATE CASCADE; EXCEPTION WHEN duplicate_object THEN NULL; END $$;

CREATE TABLE IF NOT EXISTS "PayrollParam" (
  "id" TEXT NOT NULL DEFAULT 'singleton', "ivssBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "faovBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "incesBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "otDayFactor" DOUBLE PRECISION NOT NULL DEFAULT 1.5, "otNightFactor" DOUBLE PRECISION NOT NULL DEFAULT 1.3,
  "monthDays" INTEGER NOT NULL DEFAULT 30, "weeklyHours" INTEGER NOT NULL DEFAULT 40,
  "updatedAt" TIMESTAMP(3) NOT NULL, CONSTRAINT "PayrollParam_pkey" PRIMARY KEY ("id"));

CREATE TABLE IF NOT EXISTS "PayrollRun" (
  "id" TEXT NOT NULL, "number" TEXT, "type" TEXT NOT NULL, "periodFrom" TIMESTAMP(3) NOT NULL,
  "periodTo" TIMESTAMP(3) NOT NULL, "exchangeRate" DOUBLE PRECISION NOT NULL,
  "status" TEXT NOT NULL DEFAULT 'DRAFT', "totalGrossBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "totalDeductionsBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "totalNetBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "createdById" TEXT NOT NULL, "closedAt" TIMESTAMP(3),
  "createdAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP, "updatedAt" TIMESTAMP(3) NOT NULL,
  CONSTRAINT "PayrollRun_pkey" PRIMARY KEY ("id"));
CREATE UNIQUE INDEX IF NOT EXISTS "PayrollRun_number_key" ON "PayrollRun"("number");

CREATE TABLE IF NOT EXISTS "PayrollRunLine" (
  "id" TEXT NOT NULL, "payrollRunId" TEXT NOT NULL, "employeeId" TEXT NOT NULL,
  "salaryBaseUsd" DOUBLE PRECISION NOT NULL, "daysWorked" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "daysRest" DOUBLE PRECISION NOT NULL DEFAULT 0, "overtimeDayHours" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "overtimeNightHours" DOUBLE PRECISION NOT NULL DEFAULT 0, "manualDeductionUsd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "creditDeductionBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "salaryBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "overtimeBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "grossBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "ivssBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "faovBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "totalDeductionsBs" DOUBLE PRECISION NOT NULL DEFAULT 0, "netBs" DOUBLE PRECISION NOT NULL DEFAULT 0,
  "netUsd" DOUBLE PRECISION NOT NULL DEFAULT 0,
  CONSTRAINT "PayrollRunLine_pkey" PRIMARY KEY ("id"));
CREATE UNIQUE INDEX IF NOT EXISTS "PayrollRunLine_payrollRunId_employeeId_key" ON "PayrollRunLine"("payrollRunId","employeeId");
DO $$ BEGIN ALTER TABLE "PayrollRunLine" ADD CONSTRAINT "PayrollRunLine_payrollRunId_fkey" FOREIGN KEY ("payrollRunId") REFERENCES "PayrollRun"("id") ON DELETE CASCADE ON UPDATE CASCADE; EXCEPTION WHEN duplicate_object THEN NULL; END $$;
DO $$ BEGIN ALTER TABLE "PayrollRunLine" ADD CONSTRAINT "PayrollRunLine_employeeId_fkey" FOREIGN KEY ("employeeId") REFERENCES "Employee"("id") ON DELETE RESTRICT ON UPDATE CASCADE; EXCEPTION WHEN duplicate_object THEN NULL; END $$;
```

- [ ] **Step 3: Espejar en `deploy/fix-schema.sql`**

Anexar el mismo bloque `CREATE TABLE IF NOT EXISTS ...` de arriba al final de `deploy/fix-schema.sql` (red de seguridad idempotente del deploy).

- [ ] **Step 4: Aplicar migración + regenerar cliente**

```bash
pnpm --filter @trinity/database exec prisma migrate deploy
pnpm --filter @trinity/database exec prisma generate
```
Expected: `Applying migration 20260718100000_payroll_module` → `All migrations have been successfully applied.`

- [ ] **Step 5: Commit**

```bash
git add packages/database/prisma/schema.prisma "packages/database/prisma/migrations/20260718100000_payroll_module/migration.sql" deploy/fix-schema.sql
git commit -m "feat: Session X - nomina Fase 0: schema Employee/PayrollRun + migracion aditiva"
```

### Task 2: Motor de cálculo (función pura)

**Files:**
- Create: `apps/api/src/modules/payroll/payroll-calc.ts`

- [ ] **Step 1: Escribir la función pura con las fórmulas del Excel**

Crear `apps/api/src/modules/payroll/payroll-calc.ts`:

```ts
// Motor de cálculo de nómina. Función PURA (sin I/O) para poder validarla contra el Excel de RRHH.
// Reglas verificadas en docs/superpowers/plans/2026-07-17-nomina.md. NO se redondean los intermedios
// (diario, valor-hora): solo los montos Bs finales, para reproducir el Excel al centavo.

export interface PayrollParams {
  ivssBs: number;        // monto fijo IVSS por período
  faovBs: number;        // monto fijo FAOV por período
  incesBs: number;       // monto fijo INCES por período (0 por ahora)
  otDayFactor: number;   // 1.5
  otNightFactor: number; // 1.3
  monthDays: number;     // 30
  periodHours: number;   // 40 (semanal); 80 si quincenal
  periodsPerYear: number;// 52 (semanal) | 24 (quincenal)
}

export interface PayrollLineInput {
  salaryBaseUsd: number;
  daysWorked: number;
  daysRest: number;
  overtimeDayHours: number;
  overtimeNightHours: number;
  manualDeductionUsd: number;
  creditDeductionBs: number;
  rate: number; // tasa BCV
}

export interface PayrollLineResult {
  monthlyUsd: number; dailyUsd: number; totalDays: number; periodSalaryUsd: number;
  salaryBs: number; hourlyBs: number; otDayRateBs: number; otNightRateBs: number;
  otDayTotalBs: number; otNightTotalBs: number; overtimeBs: number;
  ivssBs: number; faovBs: number; incesBs: number; manualDeductionBs: number;
  creditDeductionBs: number; totalDeductionsBs: number;
  grossBs: number; netBs: number; netUsd: number;
}

const r2 = (n: number) => Math.round(n * 100) / 100;

export function computePayrollLine(input: PayrollLineInput, p: PayrollParams): PayrollLineResult {
  const monthlyUsd = (input.salaryBaseUsd * p.periodsPerYear) / 12;
  const dailyUsd = monthlyUsd / p.monthDays;
  const totalDays = input.daysWorked + input.daysRest;
  const periodSalaryUsd = totalDays * dailyUsd;
  const salaryBs = r2(periodSalaryUsd * input.rate);

  const hourlyBs = periodSalaryUsd * input.rate / p.periodHours;
  const otDayRateBs = hourlyBs * p.otDayFactor;
  const otNightRateBs = otDayRateBs * p.otNightFactor;
  const otDayTotalBs = r2(input.overtimeDayHours * otDayRateBs);
  const otNightTotalBs = r2(input.overtimeNightHours * otNightRateBs);
  const overtimeBs = r2(otDayTotalBs + otNightTotalBs);

  const worked = totalDays > 0;
  const ivssBs = worked ? p.ivssBs : 0;
  const faovBs = worked ? p.faovBs : 0;
  const incesBs = worked ? p.incesBs : 0;
  const manualDeductionBs = r2(input.manualDeductionUsd * input.rate);
  const creditDeductionBs = r2(input.creditDeductionBs);
  const totalDeductionsBs = r2(ivssBs + faovBs + incesBs + manualDeductionBs + creditDeductionBs);

  const grossBs = r2(salaryBs + overtimeBs);
  const netBs = r2(grossBs - totalDeductionsBs);
  const netUsd = input.rate > 0 ? r2(netBs / input.rate) : 0;

  return {
    monthlyUsd, dailyUsd, totalDays, periodSalaryUsd, salaryBs, hourlyBs,
    otDayRateBs, otNightRateBs, otDayTotalBs, otNightTotalBs, overtimeBs,
    ivssBs, faovBs, incesBs, manualDeductionBs, creditDeductionBs, totalDeductionsBs,
    grossBs, netBs, netUsd,
  };
}
```

- [ ] **Step 2: Typecheck**

Run: `cd apps/api && npx tsc --noEmit`
Expected: EXIT 0

### Task 3: Validación contra filas reales del Excel

**Files:**
- Create: `apps/api/_payroll-validate.ts` (script scratch, no se trackea)

- [ ] **Step 1: Escribir el script de validación con casos reales del Excel**

Crear `apps/api/_payroll-validate.ts`. Casos tomados de la hoja `NOMINA` (tasa BCV 563.29):

```ts
import { computePayrollLine, PayrollParams } from './src/modules/payroll/payroll-calc';

const P: PayrollParams = {
  ivssBs: 6.75, faovBs: 3.46, incesBs: 0,
  otDayFactor: 1.5, otNightFactor: 1.3, monthDays: 30, periodHours: 40, periodsPerYear: 52,
};
const RATE = 563.29;
let fails = 0;
const near = (label: string, got: number, exp: number, tol = 0.02) => {
  const ok = Math.abs(got - exp) <= tol;
  if (!ok) { fails++; console.log(`  ✗ ${label}: got ${got} exp ${exp}`); }
  else console.log(`  ✓ ${label}: ${got}`);
};

// CLEIDER (F13): base 50, 5+2 días, sin HE → Q=28477.44, neto=28467.23
console.log('CLEIDER (base 50, 7 días, sin HE):');
const c = computePayrollLine({ salaryBaseUsd: 50, daysWorked: 5, daysRest: 2, overtimeDayHours: 0, overtimeNightHours: 0, manualDeductionUsd: 0, creditDeductionBs: 0, rate: RATE }, P);
near('salarioBs (Q)', c.salaryBs, 28477.44);
near('netoBs (V)', c.netBs, 28467.23);

// PEDRO (F15): base 50, 7 días, 14 horas extra diurnas → W=711.94, X=1067.90, HED total=14950.66, Z=1388.28
console.log('PEDRO (base 50, 7 días, 14 HED):');
const pe = computePayrollLine({ salaryBaseUsd: 50, daysWorked: 5, daysRest: 2, overtimeDayHours: 14, overtimeNightHours: 0, manualDeductionUsd: 0, creditDeductionBs: 0, rate: RATE }, P);
near('valorHora (W)', pe.hourlyBs, 711.94, 0.05);
near('valorHED (X)', pe.otDayRateBs, 1067.90, 0.05);
near('valorHEN (Z)', pe.otNightRateBs, 1388.28, 0.05);
near('totalHED (AC/AD)', pe.otDayTotalBs, 14950.66, 0.1);

// ANDREINA (F18): base 35, 7 días → Q=19934.21
console.log('ANDREINA (base 35, 7 días):');
const a = computePayrollLine({ salaryBaseUsd: 35, daysWorked: 5, daysRest: 2, overtimeDayHours: 0, overtimeNightHours: 0, manualDeductionUsd: 0, creditDeductionBs: 0, rate: RATE }, P);
near('salarioBs (Q)', a.salaryBs, 19934.21, 0.05);

console.log(fails === 0 ? '\nTODO OK ✅' : `\n${fails} FALLOS ❌`);
process.exit(fails === 0 ? 0 : 1);
```

- [ ] **Step 2: Ejecutar la validación**

Run: `cd apps/api && npx tsx _payroll-validate.ts`
Expected: todas las líneas `✓` y `TODO OK ✅` (exit 0). Si algo falla, revisar la fórmula antes de seguir — el motor debe reproducir el Excel al centavo en los campos no ambiguos (salario, valor-hora, recargos).

- [ ] **Step 3: Documentar la regla ambigua**

En `apps/api/src/modules/payroll/payroll-calc.ts`, confirmar que el comentario de cabecera menciona: *"El Excel muestra 'A cobrar' (V) SIN horas extra; este motor calcula neto = (salario+HE) − deducciones. Confirmar con RRHH si el pago neto incluye las HE o se pagan por separado."* (Ya incluido en el código de la Task 2.)

- [ ] **Step 4: Commit**

```bash
git add apps/api/src/modules/payroll/payroll-calc.ts
git commit -m "feat: Session X - nomina Fase 0: motor de calculo puro validado contra Excel RRHH"
```

---

## Self-Review (Fase 0)

- **Cobertura:** el motor implementa todas las fórmulas del Excel (mensual, diario, salario Bs, valor-hora, HED/HEN, deducciones fijas, neto). ✓
- **Regla ambigua marcada:** V sin HE vs neto con HE → documentada + validación solo asegura los campos no ambiguos. ✓
- **Consistencia de tipos:** `computePayrollLine(PayrollLineInput, PayrollParams): PayrollLineResult` usado igual en Task 2 y Task 3. ✓
- **Pendiente para Fase 3 (no Fase 0):** semántica del sueldo base quincenal (¿es el mismo número que semanal o distinto?) — confirmar con RRHH antes de la corrida quincenal; el motor ya lo parametriza con `periodsPerYear`/`periodHours`.

---

## Handoff de ejecución

Al ejecutar, dos opciones: **(1) Subagente por tarea** (fresco por tarea, revisión entre tareas) o **(2) Inline** en esta sesión con checkpoints. La Fase 0 es corta (3 tareas) y de bajo riesgo → inline es razonable.
